# Build outputs y caches
.next
node_modules
out
dist
build

# Git
.git
.gitignore

# Env files — el image FINAL no los incluye (multi-stage Dockerfile).
# .env.production NO se excluye porque Next.js lo lee durante build (stage
# builder) para hornear las NEXT_PUBLIC_* en el bundle. El stage runner
# NO copia ese archivo, así que el image final queda limpio.
.env
.env.local
.env.development
.env*.local

# Tests + artefactos
tests
playwright-report
test-results
playwright.config.ts

# SDD / OpenSpec
openspec
docs

# Logs y editor
*.log
.DS_Store
.vscode
.idea

# Deploy artifacts
scripts/deploy.sh
scripts/server-setup.sh

# Misc
README.md
CHANGELOG.md
.prettierrc*
.eslintrc*
.prettierignore
